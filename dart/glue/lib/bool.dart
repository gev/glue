/// Bool module - Boolean operations and control flow
/// Mirrors Haskell Glue.Lib.Bool exactly

import 'ir.dart';

/// Boolean true value
final IrBool $true = IrBool(true);

/// Boolean false value
final IrBool $false = IrBool(false);

// TODO: Implement boolean operations and control flow
// - eq, ne, lt, le, gt, ge
// - not, and, or
// - if, when, while, until

// For now, just export the basic values
// This will be expanded with full boolean operations
